name: Cepheus Load Indicator
description: "Display laptop CPU, RAM, and system stats on Arduino UNO Q LED matrix"
icon: 📊
version: "1.0.0"

# Exposed network ports
ports:
  - 5000

# Optional: Define bricks if using pre-built components
bricks: []
